# Habilita o motor de reescrita de URL do Apache
RewriteEngine On

# Impede que a regra seja aplicada a arquivos e diretórios existentes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona todas as outras requisições para index.php,
# passando a URL original como um parâmetro 'url'
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]